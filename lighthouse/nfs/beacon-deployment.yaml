apiVersion: apps/v1
kind: Deployment
metadata:
  name: beacon
  namespace: lighthouse
  labels:
    app: beacon
spec:
  selector:
    matchLabels:
      app: beacon
  replicas: 1
  strategy: 
    type: Recreate
  template:
    metadata:
      labels:
        app: beacon
    spec:
      containers:
      - name: beacon
        image: index.docker.io/sigp/lighthouse:v1.3.0
        args:
        - lighthouse
        - --network=prater
        - bn
        - --staking
        - --eth1-endpoints=<goerli eth1 node>
        - --datadir=/data/lighthouse/beacon
        ports:
        - containerPort: 9000
          hostPort: 9000
          protocol: TCP
        - containerPort: 9000
          hostPort: 9000
          protocol: UDP
        - containerPort: 5052
          protocol: TCP
        volumeMounts:
        - name: beacon-storage
          mountPath: /data/lighthouse/beacon
      volumes:
      - name: beacon-storage
        nfs:
          path: /data/lighthouse/beacon
          server: 172.20.10.10
          readOnly: false
      securityContext:
        runAsUser: 1001
        runAsGroup: 2000
      serviceAccountName: beacon
      restartPolicy: Always
